import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Linked in of Linh',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
